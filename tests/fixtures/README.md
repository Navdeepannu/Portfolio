# Registry compatibility fixtures

`public-registry-names.json` records the sorted names of the individual installable artifacts that
were present in `public/r` before the Phase 1 cleanup. The aggregate `public/r/registry.json` is
excluded. This is a historical Phase 2 compatibility snapshot, not a registry source of truth.
